# Copy to .env or configure these variables in your process manager.

EEG_ENV=production
EEG_SECRET_KEY=change-this-to-a-long-random-secret
# Optional aber empfohlen: eigener Key fuer lokale Secret-Dateien wie Google OAuth.
# Erzeugen: python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
EEG_DATA_ENCRYPTION_KEY=change-this-to-a-fernet-or-long-random-secret
EEG_INITIAL_ADMIN_PASSWORD=change-this-on-first-login
EEG_TIMEZONE=Europe/Vienna

# Optional: only allow requests when a trusted proxy sets CF-IPCountry/X-Country-Code.
# EEG_ALLOWED_COUNTRIES=AT

EEG_HOST=127.0.0.1
EEG_PORT=5000
# Public base URL used for invitation links and OAuth callbacks in e-mails/UI.
EEG_PUBLIC_BASE_URL=https://deine-domain.example

EEG_ORG_NAME=EEG Portal
EEG_ORG_EMAIL=office@example.org
EEG_ORG_WEBSITE=https://example.org/
EEG_ORG_ADDRESS=Adresse bitte konfigurieren
EEG_ORG_LEGAL=Vereinsdaten bitte konfigurieren

# Optional: Google Drive Backup. Dateien bleiben lokal und dürfen nicht ins Git.
# In der Google Cloud Console als autorisierte Redirect-URI hinterlegen:
# https://deine-domain.example/admin/backup/google/callback
EEG_GOOGLE_CLIENT_SECRETS=/var/www/eeg/instance/google_client_secret.json
EEG_GOOGLE_TOKEN_FILE=/var/www/eeg/instance/google_drive_token.json
# EEG_GOOGLE_OAUTH_REDIRECT_URI=https://deine-domain.example/admin/backup/google/callback
